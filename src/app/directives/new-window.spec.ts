import { NewWindow } from './new-window';

describe('NewWindow', () => {
  it('should create an instance', () => {
    const directive = new NewWindow();
    expect(directive).toBeTruthy();
  });
});
