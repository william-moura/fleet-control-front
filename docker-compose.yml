version: '3.8'
services:
  fleet-control-front:
    image: nginx:alpine
    ports:
      - 8081:80
    volumes:
      - ./dist/fleet-control-front/browser:/usr/share/nginx/html
      - ./site.conf:/etc/nginx/conf.d/default.conf      
    # command: nginx -g 'daemon off;'